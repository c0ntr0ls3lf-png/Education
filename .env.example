# MongoDB Connection
# For local MongoDB:
MONGODB_URI="mongodb://localhost:27017/education"
# For MongoDB Atlas (replace with your connection string):
# MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/education?retryWrites=true&w=majority"

# Auth secret for JWT token signing
AUTH_SECRET="edulms_default_secret_change_in_production"
